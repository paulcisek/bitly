<% unless object.nil? %>
    <tr class="header">
        <th id="long">Long Link</th>
        <th id="short">Short Link</th>
    </tr>
    <% object.each do |link| %>
        <tr>
            <td><%=truncate(link.long_link, length: 50)%></th>
            <td><%=link_to link.short_link, link.short_link, target: 'blank'%></th>
        </tr>
    <% end %>
<% end %>
